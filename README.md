# np_producer

Simple project to send fake data with kafka

### Run the project

Download project
```bash
git clone https://github.com/ActionKlo/np_producer.git
cd np_producer
```

Run docker compose

```bash
docker compose up -d
```
